/** @type {import('next').NextConfig} */
const nextConfig = {};
const image = {
    images: {
      domains: ['img.freepik.com'], // Add your external image domain here
    },
  };
export default {nextConfig , image}


  